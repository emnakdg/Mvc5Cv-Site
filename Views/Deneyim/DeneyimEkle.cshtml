@model MvcCv.Models.Entity.TblDeneyimlerim
@{
    ViewBag.Title = "DeneyimEkle";
    Layout = "~/Views/Shared/_AdminLayout.cshtml";
}

<!DOCTYPE html>
<html>
<body class="hold-transition sidebar-mini">
    <div class="wrapper">
        <!-- Navbar -->
        <!-- Main content -->
        <section class="content">
            <div class="container-fluid">
                <div class="row">
                    <div class="col-md-12">
                        <div class="card">
                            <div class="card-header">
                                <h2 class="card-text">Yeni Deneyim Ekleme Sayfası</h2>
                            </div>
                            <!-- /.card-header -->
                            <div class="card-body">
                                <form method="post">
                                    @Html.Label("Şirket")
                                    @Html.TextBoxFor(x => x.BASLIK, new { @class = "form-control" })
                                    @Html.ValidationMessageFor(x => x.BASLIK, "", new { @style = "color:red" })
                                    <br />
                                    @Html.Label("Pozisyon Açıklaması")
                                    @Html.TextBoxFor(x => x.ALTBASLIK, new { @class = "form-control" })
                                    @Html.ValidationMessageFor(x => x.ALTBASLIK, "", new { @style = "color:red" })
                                    <br />
                                    @Html.Label("Deneyim Açıklaması")
                                    @Html.TextBoxFor(x => x.ACIKLAMA, new { @class = "form-control" })
                                    @Html.ValidationMessageFor(x => x.ACIKLAMA, "", new { @style = "color:red" })
                                    <br />
                                    @Html.Label("Tarih")
                                    @Html.TextBoxFor(x => x.TARIH, new { @class = "form-control" })
                                    @Html.ValidationMessageFor(x => x.TARIH, "", new { @style = "color:red" })
                                    <br />
                                    <button class="btn btn-info">Kaydet</button>
                                </form>
                            </div>
                        </div>
                        <!-- /.card -->
                    </div>
                </div>
            </div><!-- /.container-fluid -->
        </section>
        <!-- /.content -->
    </div>
</body>
</html>

